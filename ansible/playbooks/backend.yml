---
# Deploys Django + Gunicorn on [app] hosts.
# Runs after database.yml and cache.yml so the DB/Redis are ready.

- name: Deploy Django backend
  hosts: app
  become: true
  roles:
    - django-app
