---
- name: restart postgresql
  systemd:
    name: "postgresql@{{ postgresql_version }}-main"
    state: restarted

- name: reload postgresql
  systemd:
    name: "postgresql@{{ postgresql_version }}-main"
    state: reloaded
